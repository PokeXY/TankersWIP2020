using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MarisaStats : MonoBehaviour
{
    // Marisa's Stats
    public int power = 5;
    public int defense = 2;
    public int speed = 3;
    public int bombPower = 10;
    public int exp = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
